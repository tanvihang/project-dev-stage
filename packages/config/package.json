{
  "name": "@devstage/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration for DevStage",
  "main": "./index.js",
  "scripts": {
    "clean": "rm -rf .turbo node_modules"
  }
}
